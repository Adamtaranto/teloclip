name: teloclip
channels:
  - conda-forge
  - bioconda
dependencies:
  - bioconda::winnowmap
  - minimap2
  - pip
  - python >=3.12
  - samtools
  - pip:
      - click
      - hatch
      - isort
      - numpydoc-validation
      - pre-commit
      - pytest
      - pytest-cov
      - ruff
